The assumed application language in this context is Java.

For linting we could consider using tools like SonarQube, SpotBugs and CheckStyle.
For testing our Java application, we would consider using JUnit which is the most popular or Mockito and TestNG.
For building the project in Java, tools like Maven or Gradle are usually used. Bazel is also an option.

Alternatives to CI setups besides Jenkins and GitHub Actions are GitLab CI/CD, Azure Devops and Amazon AWS.

For this project, we would prefer to host it in a cloud-based environment. This is becasue the team is small and the project is most likely not a large one so going throught the hassle of managing the infrastructure by ourself would be unnecessary. Also a cloud solution wold be easier to maintain and scale when we progres. It would also be much cheaper for us.
Hosted in a cloud environment or not?
